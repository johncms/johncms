<div class="phdr">
    <strong><a href="?"><?= _t('Library') ?></a></strong> | <?= _t('Delete') ?>
</div>
<?php if ($error) : ?>
    <?= $error ?>
<?php endif; ?>
<?php if ($dirchange) : ?>
    <?php if ($mode === 'moveaction') : ?>
        <?php if ($moving) : ?>
            <div class="gmenu"><?= _t('Successful transfer') ?></div>
            <div>
                <a href="?do=dir&amp;id=<?= $move ?>"><?= _t('Back') ?></a>
            </div>
        <?php else : ?>
            <div class="alarm">
                <div><?= _t('Are you sure you want to move the contents?') ?></div>
                <div>
                    <a href="?act=del&amp;type=<?= $type ?>&amp;id=<?= $id ?>&amp;movedeny&amp;do=moveaction&amp;move=<?= ($_POST['move']) ?>"><?= _t('Move') ?></a> |
                    <a href="?"><?= _t('Cancel') ?></a>
                </div>
            </div>
        <?php endif; ?>
    <?php endif; ?>
    <?php if ($mode === 'delmove') : ?>
        <?php if ($delmove) : ?>
            <div class="menu">
                <h3><?= _t('Move to Section') ?></h3>
                <form action="?act=del&amp;type=dir&amp;id=<?= $id ?>" method="post">
                    <div>
                        <select name="move">
                            <?php foreach ($delmove as $itemId => $itemName) : ?>
                                <option value="<?= $itemId ?>"><?= $itemName ?></option>
                            <?php endforeach; ?>
                        </select>
                    </div>
                    <div>
                        <input type="hidden" name="mode" value="moveaction"/>
                    </div>
                    <div class="bmenu">
                        <input type="submit" name="submit" value="<?= _t('Approve') ?>"/>
                    </div>
                </form>
            </div>
        <?php else : ?>
            <div class="rmenu"><?= _t('There are no Sections for moving') ?></div>
            <div class="bmenu">
                <a href="?"><?= _t('Back') ?></a>
            </div>
        <?php endif; ?>
    <?php endif; ?>
    <?php if ($mode === 'delall') : ?>
        <?php if ($deldeny) : ?>
            <div class="gmenu"><?= $deldeny ?></div>
            <div><a href="?"><?= _t('Back') ?></a></div>
        <?php else : ?>
            <div class="alarm">
                <div><?= _t('Are you sure you want to delete content?') ?></div>
                <div>
                    <a href="?act=del&amp;type=<?= $type ?>&amp;id=<?= $id ?>&amp;deldeny&amp;do=delall"><?= _t('Delete') ?></a> |
                    <a href="?"><?= _t('Cancel') ?></a>
                </div>
            </div>
        <?php endif; ?>
    <?php endif; ?>
    <?php if (! $mode) : ?>
        <div class="alarm"><?= _t('Section is not empty') ?></div>
        <div class="menu">
            <h3><?= _t('Select action') ?></h3>
            <form action="?act=del&amp;type=dir&amp;id=<?= $id ?>" method="post">
                <div>
                    <input type="radio" name="mode" value="delmove" checked="checked"/> <?= _t('Delete with movement') ?>
                </div>
                <div>
                    <input type="radio" name="mode" value="delall"/> <span style="color: red;"> <?= _t('Delete all Sections and Articles') ?></span>
                </div>
                <div class="bmenu">
                    <input type="submit" name="submit" value="<?= _t('Do') ?>"/>
                </div>
            </form>
        </div>
    <?php endif; ?>
<?php else : ?>
    <?php if (! isset($_GET['yes'])) : ?>
        <div class="alarm">
            <div><?= _t('Delete confirmation') ?></div>
            <div>
                <a href="?act=del&amp;type=<?= $type ?>&amp;id=<?= $id ?>&amp;yes"><?= _t('Delete') ?></a> |
                <a href="?do=dir&amp;id=<?= $id ?>"><?= _t('Cancel') ?></a>
            </div>
        </div>
    <?php endif; ?>
<?php endif; ?>

<?php if ($type === 'article') : ?>
    <?php if (! $article) : ?>
        <div class="rmenu">
            <p><?= _t('Delete confirmation') ?>
                <br>
                <a href="?act=del&amp;type=<?= $type ?>&amp;id=<?= $id ?>&amp;yes"><?= _t('Delete') ?></a> |
                <a href="?id=<?= $id ?>"><?= _t('Cancel') ?></a>
            </p>
        </div>
    <?php endif; ?>
<?php endif; ?>

<?php if ($type === 'image') : ?>
    <?php if (! $image) : ?>
        <div class="alarm">
            <div><?= _t('Delete confirmation') ?></div>
            <div>
                <a href="?act=del&amp;type=<?= $type ?>&amp;id=<?= $id ?>&amp;yes"><?= _t('Delete') ?></a> |
                <a href="?act=moder&amp;type=article&amp;id=<?= $id ?>"><?= _t('Cancel') ?></a>
            </div>
        </div>
    <?php else : ?>
        <div class="gmenu"><?= _t('Deleted') ?></div>
        <div>
            <a href="?act=moder&amp;type=article&amp;id=<?= $id ?>"><?= _t('Back') ?></a>
        </div>
    <?php endif; ?>
<?php endif; ?>
<?php if ($deny) : ?>
    <div class="gmenu"><?= _t('Deleted') ?></div>
    <p>
        <a href="?"><?= _t('Back') ?></a>
    </p>
<?php endif; ?>
