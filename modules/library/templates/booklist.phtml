<!-- Page switching -->
<?php if ($total > $user->config->kmess) : ?>
    <div><?= $pagination ?></div>
<?php endif ?>

<?php if ($total) : ?>
    <!-- Listing Sections -->
    <?php foreach ($list() as $booklist): ?>
        <?php $i = 0; ?>
        <div class="list<?= ((++$i % 2) ? 2 : 1) ?>">
            <?php if ($booklist['cover']) : ?>
                <div class="avatar"><img src="../upload/library/images/small/<?= $booklist['id'] ?>.png" alt="screen"/></div>
            <?php endif; ?>
            <div class="righttable"><h4><a href="?id=<?= $booklist['id'] ?>"><?= $booklist['name'] ?></a></h4>
                <div><small><?= $booklist['announce'] ?></small></div>
            </div>
            <table class="desc">
                <?php if ($booklist['tags']) : ?>
                <tr>
                    <td class="caption"><?= __('The Tags') ?>:</td>
                    <td><?= ($booklist['tags']) ?></td>
                </tr>
                    <?php endif; ?>
                <tr>
                    <td class="caption"><?= __('Who added') ?>:</td>
                    <td><?= $booklist['who'] ?></td>
                </tr>
                <tr>
                    <td class="caption"><?= __('Rating') ?>:</td>
                    <td><?= $booklist['ratingView'] ?></td>
                </tr>
            </table>
        </div>
    <?php endforeach; ?>
<?php else : ?>
    <div class="menu"><?= __('The list is empty') ?></div>
<?php endif; ?>
<div class="phdr"><?= __('Total') ?>: <?= $total ?></div>
<!-- Page switching -->
<?php if ($total > $user->config->kmess) : ?>
    <div><?= $pagination ?></div>
<?php endif ?>
<?php if ($moderMenu) : ?>
    <p>
    <a href="?act=addnew&amp;id=<?= $id ?>"><?= __('Write Article') ?></a>
    <?php if ($admin) : ?>
        <br>
        <a href="?act=moder&amp;type=dir&amp;id=<?= $id ?>"><?= __('Edit') ?></a>
        <br>
        <a href="?act=del&amp;type=dir&amp;id=<?= $id ?>"><?= __('Delete') ?></a>
        </p>
    <?php endif; ?>
<?php endif; ?>
