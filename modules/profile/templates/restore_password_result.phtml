<?php $this->layout(
    'system::layout/default',
    [
        'title'      => __('Restore password'),
        'page_title' => __('Restore password'),
    ]
) ?>

<form action="skl.php?act=sent" method="post">
    <h4><?= __('Restore password') ?></h4>
    <?= $this->fetch(
        'system::app/alert',
        [
            'alert_type' => $type == 'success' ? 'alert-success' : 'alert-danger',
            'alert'      => $message,
        ]
    ) ?>
    <?php if ($type == 'error'): ?>
        <a href="skl.php" class="btn btn-primary"><?= __('Back') ?></a>
    <?php endif; ?>
</form>
