<?php $this->layout('system::layout/default', [
        'title'      => _t('Login', 'system'),
        'page_title' => _t('Login', 'system'),
    ]
) ?>
<div class="row">
    <div class="m-sm-auto m-md-auto m-lg-0 col-sm-8 col-md-8 col-lg-7 col-xl-5">
        <div class="card shadow border-0 border-radius-12">
            <form method="post">
                <div class="card-body">
                    <h4 class="card-title"><?= _t('Authorization', 'system') ?></h4>
                    <?php if (! empty($error)): ?>
                        <?= $this->insert('system::app/alert', ['alert_type' => 'alert-danger', 'alert' => $error]) ?>
                    <?php endif ?>
                    <div class="form-group">
                        <label for="login"><?= _t('Username', 'system') ?></label>
                        <input type="text"
                               class="form-control"
                               maxlength="20"
                               name="n"
                               id="login"
                               required
                               value="<?= $this->e($user_login) ?>"
                               placeholder="<?= _t('Username', 'system') ?>"
                        >
                    </div>
                    <div class="form-group">
                        <label for="password"><?= _t('Password', 'system') ?></label>
                        <input
                            type="password"
                            class="form-control"
                            name="p"
                            maxlength="20"
                            id="password"
                            required
                            placeholder="<?= _t('Password', 'system') ?>"
                        >
                    </div>
                    <div class="form-group">
                        <div class="custom-control custom-checkbox">
                            <input id="remember" type="checkbox" name="mem" value="1" checked="checked" class="custom-control-input">
                            <label for="remember" class="custom-control-label">
                                <?= _t('Remember', 'system') ?>
                            </label>
                        </div>
                    </div>
                    <button type="submit" name="login" value="1" class="btn btn-fill btn-primary"><?= _t('Login', 'system') ?></button>
                    <a href="/profile/skl.php?continue" class="btn btn-link"><?= _t('Forgot password?', 'system') ?></a>
                    <div class="mt-3">
                        <a href="/registration" class="mr-2"><?= _t('Registration', 'system') ?></a>
                    </div>
                </div>
            </form>
        </div> <!-- end card -->
    </div>
</div>
