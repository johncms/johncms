<?php

/**
 * @var Mobicms\Render\Template\Template $this
 * @var list<string> $bad_files
 * @var string $back_url
 */

$this->layout('system::layout/default');
?>

<div class="alert alert-danger">
    <div class="pb-2">
        <?= __('Snapshot Inconsistency<br>Warning! You need to pay attention to all files from the list. They have been added or modified since the image created.') ?>
    </div>
</div>

<div class="mb-3">
    <h3><?= __('List of files') ?></h3>
    <ul>
        <?php foreach ($bad_files as $file): ?>
            <li><?= $this->e($file) ?></li>
        <?php endforeach; ?>
    </ul>
</div>
<div>
    <a href="<?= $this->e($back_url) ?>" class="btn btn-outline-secondary"><?= __('Back') ?></a>
</div>
