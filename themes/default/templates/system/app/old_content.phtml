<?php

/**
 * Подключаем Layout
 */
$this->layout('system::layout/old_default', [
    'title'      => $this->e(isset($title) ? $title : $config['copyright']),
    'homeButton' => (isset($headmod) ? true : false)
]);

/**
 * Выводим содержимое старых модулей
 *
 * @var string $content
 */
echo $content;
